<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h5">Edit bottle</MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField @bind-Value="@Bottle.Vintage" Label="Vintage"/>
        
        <MudNumericField @bind-Value="@Bottle.Price" Format="N2" Label="Price" Class="mt-4"/>

        <MudSelect T="BottleSize"
                   @bind-Value="Bottle.BottleSize"
                   Label="Bottle size"
                   For="@(() => Bottle.BottleSize)"
                   Class="mt-4">
            @foreach (var item in Enum.GetValues(typeof(BottleSize)))
            {
                <MudSelectItem T="BottleSize" Value="(BottleSize)item">@item</MudSelectItem>
            }
            
            <MudDatePicker Label="In cellar since" @bind-Date="Bottle.AddedOn" Class="mt-4"/>
        </MudSelect>
    </DialogContent>
    <DialogActions>
        <MudButton ButtonType="ButtonType.Button"
                   Variant="Variant.Outlined"
                   StartIcon="@Icons.Material.Outlined.Cancel"
                   Color="Color.Primary"
                   OnClick="Cancel"
                   Class="mt-4">
            Cancel
        </MudButton>
        <MudButton Class="ml-4 mt-4"
                   ButtonType="ButtonType.Button"
                   Variant="Variant.Filled"
                   StartIcon="@Icons.Material.Outlined.Save"
                   Color="Color.Primary"
                   OnClick="EditBottle">
            Save
        </MudButton>
    </DialogActions>
</MudDialog>