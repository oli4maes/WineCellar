<MudDataGrid T="WineryDto"
             Items="Wineries"
             SortMode="SortMode.Single"
             QuickFilter="QuickFilter"
             Hideable=true
             LoadingProgressColor="Color.Primary"
             Dense="true">
    <ToolBarContent>
        <MudText Typo="Typo.h5" Color="Color.Primary">Wineries</MudText>
        <MudSpacer/>
        <MudTextField @bind-Value="_searchString"
                      Placeholder="Search"
                      Adornment="Adornment.Start"
                      Immediate="true"
                      AdornmentIcon="@Icons.Material.Outlined.Search"
                      IconSize="Size.Medium"
                      Class="mt-0">
        </MudTextField>
    </ToolBarContent>
    <Columns>
        <PropertyColumn Property="x => x.Name" Title="Name" Sortable="true" Filterable="false"/>
        <PropertyColumn Property="x => x.CountryName" Title="Country" Sortable="true" Filterable="false"/>
        <TemplateColumn Filterable="false" Sortable="false" CellClass="d-flex justify-end">
            <CellTemplate Context="WineryEditContext">
                @{
                    var winery = WineryEditContext.Item;
                }
                <MudIconButton Size="Size.Small" Icon="@Icons.Material.Outlined.Edit" Color="Color.Info" @onclick="() => OpenWinery(winery)"/>
                <MudIconButton Size="Size.Small" Icon="@Icons.Material.Outlined.Delete" Color="Color.Error" @onclick="() => DeleteWinery(winery)"/>
            </CellTemplate>
        </TemplateColumn>
    </Columns>
    <PagerContent>
        <MudDataGridPager T="WineryDto"/>
    </PagerContent>
</MudDataGrid>